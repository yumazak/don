{
  "name": "don",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --compile --outfile dist/don",
    "lint": "oxlint --type-aware --type-check --tsconfig tsconfig.json --deny-warnings src/",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "incur": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxlint": "latest",
    "oxlint-tsgolint": "^0.15.0"
  }
}
